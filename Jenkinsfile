pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh 'whoami'
                sh 'date'
            }
        }
    }
}
